local({r <- getOption("repos")
       r["CRAN"] <- "https://cloud.r-project.org" 
              options(repos=r)
})

message(paste0("Load ", getwd(), "/.Rprofile"))
require(BiocManager)
require(colorout)
